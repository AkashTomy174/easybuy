def send_whatsapp(phone_number, message):
    """
    Integrate with your WhatsApp API provider.
    """
    # Example: call Twilio/other provider
    print(f"Sending WhatsApp to {phone_number}: {message}")


def send_email(email, subject, message):
    """
    Integrate with your email provider (SMTP, SendGrid, etc.).
    """
    print(f"Sending Email to {email}: {subject} - {message}")